# 2026-06-07 Daily Log

## v4 增量开发

### E-02 主题增强 — 确认已实现
代码中 E-02 三任务均已预置完成，无需额外修改：
- StatusBar 主题快捷切换（Sun/Moon/Monitor 图标，三态循环）
- SettingsPage 外观分区（ToggleButtonGroup 替换 Select）
- 深色模式 scrollbar/Drawer 微调

### E-03 开机自启完善 — ✅ 完成
- 新增 `electron/services/auto-launch-service.ts`（封装 auto-launch@^5.0.6）
- `electron/main.ts`：替换 `app.setLoginItemSettings()` → AutoLaunchService，启动时自检修复
- `electron/preload.ts`：新增 `app.getAutoStartStatus`
- `src/types/electron.ts`：扩展 ElectronAPI 和 IPC_CHANNELS
- `src/store/settingsStore.ts`：新增 `autoStartLoading` 状态，失败回弹
- `src/pages/SettingsPage.tsx`：Switch 加 loading + Snackbar 错误提示
- i18n：新增 `settings.autoStartError` key
- 构建验证通过 ✅

### E-04 通知提醒 — ✅ 完成
- 新增 `electron/services/reminder-service.ts`（60s 检查周期，番茄钟 + 久坐累积计算）
- 新增 `src/hooks/useReminder.ts`（IPC 订阅 → Web Notification）
- `electron/tracker.ts`：新增 `TrackerCallbacks` 接口，状态变化回调
- `electron/main.ts`：集成 ReminderService 初始化/启动/清理，settings:get-all 扩展 4 个 key
- `electron/preload.ts`：新增 `reminder.onPomodoro` / `reminder.onSedentary`
- `src/types/electron.ts`：扩展 ElectronAPI + IPC_CHANNELS
- `src/store/settingsStore.ts`：新增 4 字段 + 4 actions + SETTINGS_KEYS + loadSettings + reset
- `src/App.tsx`：挂载 `useReminder()`
- `src/pages/SettingsPage.tsx`：新增「提醒」分区（番茄钟 + 久坐 Switch + Select）
- i18n：新增 settings.notifications/pomodoro/sedentary 和 reminder.* keys
- 构建验证通过 ✅

### E-01 首次使用引导 — ✅ 完成
- 新增 `src/components/OnboardingWizard.tsx`（3 步引导：Welcome → Privacy → Start，MUI MobileStepper）
- `src/App.tsx`：ThemedApp 层接入 onboarding 检查（localStorage），onboarding 期间不挂载 AppContent（绕过 useReminder）
- `src/pages/SettingsPage.tsx`：About 分区新增「重新引导」按钮（清除 localStorage + reload）
- i18n：新增 `onboarding.*` 和 `settings.restartOnboarding` keys
- 构建验证通过 ✅

## 浮动小窗 — ✅ + NSIS 打包运行中
- 新建 electron/floating-window.ts：小型 Always-on-Top 窗口，显示当前应用 + 专注状态 + 今日时长
- StatusBar 右侧新增 PictureInPicture2 图标按钮，一键显/隐
- 主进程 2s 周期推送数据到浮动窗口
- npm run package:win 后台执行中
## 打包产出
- win-unpacked/Screen Time Monitor.exe (181MB) — 免安装直接运行
- Screen-Time-Monitor-v4.0.0-portable-x64.zip (117MB) — 便携压缩包
- NSIS 安装包需安装 NSIS（npx choco install nsis）
